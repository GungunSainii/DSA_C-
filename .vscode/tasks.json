{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile and Run with IO (Windows)",
      "type": "shell",
      "command": "cmd.exe",
      "args": [
        "/c",
        "g++ -std=c++17 \"${file}\" -o \"${fileBasenameNoExtension}\" && \"${fileBasenameNoExtension}\" < input.txt > output.txt"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
